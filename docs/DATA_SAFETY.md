# Data Safety

The key business files are:

- `database/bam_motor_group.db`
- everything inside `uploads/`

Back up both regularly. Keep at least one backup outside the computer, such as
an external drive or a properly configured cloud storage folder.

The local development server is intended for use on your own computer. Do not
expose port 5000 directly to the public internet.
